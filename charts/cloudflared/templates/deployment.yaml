apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared
  namespace: cloudflared
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflared
  template:
    metadata:
      labels:
        app: cloudflared
    spec:
      containers:
        - name: cloudflared
          image: cloudflare/cloudflared:latest
          args:
            - tunnel
            - run
            - --config
            - /etc/cloudflared/config.yaml
            - --cred-file
            - /root/.cloudflared/756a650b-cd96-4323-a5c1-6dfc10564aa3.json
          volumeMounts:
            - name: config
              mountPath: /etc/cloudflared/
      volumes:
        - name: config
          configMap:
            name: cloudflared-config
        - name: creds
          secret:
            secretName: cloudflared-credentials

